# UI Design Tokens — [PROJECT NAME]

> **This file is a template.** It is generated by `init-design-system` during `init-project` and filled with the resolved token values for this project's design system.

---

## Component library: [LIBRARY NAME]

## Token Reference (Light / Dark)

### Surfaces and text

| Token class      | Light value | Dark value |
|------------------|-------------|------------|
| `bg-page`        | #... | #... |
| `bg-panel`       | #... | #... |
| `bg-panel-hover` | #... | #... |
| `text-strong`    | #... | #... |
| `text-soft`      | #... | #... |
| `text-faint`     | #... | #... |
| `bg-hero-from`   | #... | #... |
| `bg-hero-to`     | #... | #... |

### Brand

| Token class    | Value |
|----------------|-------|
| `primary`      | #... |
| `accent`       | #... |

### Interactive — Buttons

At least one button style must be defined (see `rules/ui/interactive-affordance.md`).

| Token class             | Value | Hover | Focus-visible | Disabled |
|-------------------------|-------|-------|---------------|----------|
| `button-primary-bg`     | #...  | #...  | uses `focus-ring` | opacity 0.5 |
| `button-primary-text`   | #...  | #...  | —             | #...    |
| `button-outline-border` | #...  | #...  | uses `focus-ring` | opacity 0.5 |
| `button-outline-text`   | #...  | #...  | —             | #...    |

### Interactive — Links

| Token class | Light | Dark | Default decoration | Hover decoration |
|-------------|-------|------|--------------------|------------------|
| `link-text` | #...  | #... | `underline`        | `none`           |

`link-text` must differ from `text-strong` (WCAG 1.4.1) and must be underlined by default — color alone is not sufficient.

### Borders, separators, focus

| Token class    | Value | Notes |
|----------------|-------|-------|
| `border-card`  | #... | 3:1 against `bg-page` |
| `border-input` | #... | 3:1 against `bg-panel` |
| `divider`      | #... | 3:1 against `bg-page` |
| `focus-ring`   | #... | 3:1 against every surface it overlays, including `button-primary-bg` |

## WCAG Contrast Reference

This table is generated by `check-contrast` Mode 2 from the canonical matrix. Every row must show ✅ — `init-design-system` does not close until they do.

| Pair                                              | Ratio | Required | Status |
|---------------------------------------------------|-------|----------|--------|
| `text-strong` on `bg-page` (light)                | ?:1   | 4.5:1    | [✅/❌]|
| `text-soft` on `bg-page` (light)                  | ?:1   | 4.5:1    | [✅/❌]|
| `text-strong` on `bg-panel` (light)               | ?:1   | 4.5:1    | [✅/❌]|
| `text-soft` on `bg-panel` (light)                 | ?:1   | 4.5:1    | [✅/❌]|
| `link-text` on `bg-panel` (light)                 | ?:1   | 4.5:1    | [✅/❌]|
| `button-primary-text` on `button-primary-bg`      | ?:1   | 4.5:1    | [✅/❌]|
| `button-outline-text` on `bg-page`                | ?:1   | 4.5:1    | [✅/❌]|
| `button-primary-bg` on `bg-page`                  | ?:1   | 3:1      | [✅/❌]|
| `button-primary-bg` on `bg-panel`                 | ?:1   | 3:1      | [✅/❌]|
| `button-outline-border` on `bg-page`              | ?:1   | 3:1      | [✅/❌]|
| `border-card` on `bg-page`                        | ?:1   | 3:1      | [✅/❌]|
| `border-input` on `bg-panel`                      | ?:1   | 3:1      | [✅/❌]|
| `divider` on `bg-page`                            | ?:1   | 3:1      | [✅/❌]|
| `focus-ring` on `bg-page`                         | ?:1   | 3:1      | [✅/❌]|
| `focus-ring` on `bg-panel`                        | ?:1   | 3:1      | [✅/❌]|
| `focus-ring` on `button-primary-bg`               | ?:1   | 3:1      | [✅/❌]|
| `text-white` on hero gradient                     | ?:1   | 4.5:1    | [✅/❌]|
[dark-variant rows appended by init-design-system]

## Affordance Audit (check-affordance)

Run automatically by `init-design-system` paso 7. Every check must show ✅.

| # | Check                                                          | Status |
|---|----------------------------------------------------------------|--------|
| 1 | At least one button style fully defined                         | [✅/❌]|
| 2 | `button-primary-bg` distinguishable from `bg-page` and `bg-panel` | [✅/❌]|
| 3 | `button-primary-text` defined alongside `button-primary-bg`    | [✅/❌]|
| 4 | `button-outline-text` defined alongside `button-outline-border`| [✅/❌]|
| 5 | `link-text` ≠ `text-strong` (light + dark)                     | [✅/❌]|
| 6 | `focus-ring` defined                                            | [✅/❌]|
| 7 | `border-card` and `border-input` defined                        | [✅/❌]|
| 8 | Links underlined by default                                     | [✅/❌]|
| 9 | Hover + focus-visible declared for every interactive token     | [✅/❌]|
| 10| Disabled state declared for every button                        | [✅/❌]|

## Pattern Catalogue

### Hero / dark sections
[filled by init-design-system based on chosen palette]

### Buttons — all variants × all states
[Primary default / hover / focus-visible / disabled — filled by init-design-system]
[Outline default / hover / focus-visible / disabled — filled by init-design-system]

### Links — default and hover
[filled by init-design-system]

### Inputs — default / focus / disabled / error
[filled by init-design-system]

### Focus ring — illustrated on at least two surface combinations
[filled by init-design-system]

### Status badges — and how they differ visually from buttons
[filled by init-design-system]

### Active tab / selector states
[filled by init-design-system]

## Contrast & Affordance Audit Checklist

- [ ] Hero sections — all text uses light variants on dark gradient
- [ ] Active tabs/nav — use `text-primary` (not a low-contrast variant) on light backgrounds
- [ ] Status badges — use darker text variants in light mode with dark-mode overrides
- [ ] Normal text on panels — use `text-strong` or `text-soft`; never `text-faint` for meaningful content
- [ ] Every interactive token has hover, focus-visible, and disabled states declared
- [ ] Links underlined by default; color alone is never sufficient
- [ ] Informational sections do not mimic button affordances (pill radius, fill, ring)
